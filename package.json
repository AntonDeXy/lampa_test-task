{
  "scripts": {
    "start:dev": "cd client && npm run start:express & cd server && npm start",
    "start": "cd client && npm i && REACT_APP_EXPRESS_SERVER=true npm run build && cd ../server && npm i && npm start"
  }
}
